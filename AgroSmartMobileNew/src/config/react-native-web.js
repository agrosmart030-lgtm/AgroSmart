// Configurações específicas para React Native Web

// Mapeamento de componentes nativos para web
const components = {
  // Adicione mapeamentos específicos de componentes aqui
};

// Configurações de estilos para web
const styles = {
  // Estilos globais para web
  body: {
    margin: 0,
    padding: 0,
    fontFamily: `-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif`,
    WebkitFontSmoothing: 'antialiased',
    MozOsxFontSmoothing: 'grayscale',
  },
  // Adicione mais estilos conforme necessário
};

export { components, styles };
